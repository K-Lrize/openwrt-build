# GL-MT3600BE 包清单 — G2 套餐方案
#
# 语法:
#   @preset <name>   引用 common/presets/<name>.list 全部包
#   +<pkg>           装这个包 (套餐外个性加)
#   -<pkg>           从 IB DEFAULT_PACKAGES 排除 (避免 multi-variant 冲突)

# ---- 套餐 ----
@preset core
@preset network
@preset ipv6
@preset proxy
@preset mobile
@preset cli
@preset monitoring
@preset maintenance

# ---- 排除 IB 默认包 (避免冲突) ----
# IB DEFAULT_PACKAGES 里硬编了 dnsmasq, 用 dnsmasq-full 替代时必须 -dnsmasq
-dnsmasq

# Wi-Fi 多变体: maintenance preset 已装 wpad-openssl, 排掉所有 basic/mbedtls 变体
-wpad-basic
-wpad-basic-mbedtls
-wpad-basic-wolfssl
-wpad-mbedtls
-wpad-wolfssl
